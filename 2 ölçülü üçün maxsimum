int max(int a[100][100], int r, int c) {
    int m = a[0][0];
    for (int i = 0; i < r; i=i+1)
        for (int j = 0; j < c; j=j+1)
            if (a[i][j] > m)
                m = a[i][j];
    return m;
}